```swift
// define
var temp: [Int] = Array()
var temp = Array<Int>()
var temp = [Int]()
var temp: [[Int]] = Array()
var temp = Array<Array<Int>>()
var temp = [[Int]]()
var temp: [Int] = Array(repeating: 0, count: 3)

// 模拟队列
temp.removeFirst() // 出队
let front = temp.removeFirst() // 出队并获取队头
temp.append() // 入队

// 常用操作
let first = temp.first // 第一个元素 ?

```

